# Сетевые запросы
requests

# HTML-парсинг
beautifulsoup4

# Google Drive API
google-api-python-client
google-auth-httplib2
google-auth-oauthlib

# Переменные окружения (.env)
python-dotenv

# Транслитерация названий файлов
python-slugify

# Тестирование
pytest

# ─── Необязательно ───────────────────────────────────────────────────────────
# g4f — старый способ обращения к ИИ (напрямую к чужим сервисам).
# Нужен только как запасной вариант, если Cloudflare Worker ещё не настроен.
# Основной путь — свой Worker (см. worker/README.md).
# g4f
